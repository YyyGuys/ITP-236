<UserControl x:Class="SchoolWpfApp.Views.StudentDetailView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">

    <StackPanel Margin="20">
        <TextBlock Text="{Binding Student.FullName}" FontSize="20" FontWeight="Bold" />
        <TextBlock Text="Major:" />
        <TextBox Text="{Binding Student.Major}" Margin="0,10" />
        <TextBlock Text="Email:" />
        <TextBox Text="{Binding Student.Email}" Margin="0,10" />
        <TextBlock Text="Phone:" />
        <TextBox Text="{Binding Student.PhoneNumber}" Margin="0,10" />

        <TextBlock Text="GPA:" />
        <TextBlock Text="{Binding Student.GPA, StringFormat={}{0:F2}}" />

        <Button Content="Save"
                Command="{Binding SaveCommand}"
                Margin="0,20,0,0" />

        <Button Content="Back"
                Command="{Binding BackCommand}" />
    </StackPanel>
</UserControl>